<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To do list</title>
    <link rel="stylesheet" href="css/styles.css">
</head>

<body>
    <!--for writing any sort of javascript in ejs every line should start and end with % tag evenif it is a single curly bracket then too we need to enclose within the tags and only for control flow ie if else statements -->
    <div class="box" id="heading">
        <h1>Hey today is <%= nameOfDay %>
        </h1>
    </div>



    <div class="box">
        <% items.forEach(function(item){ %>
            <div class="item">
                <form class="item" action="/delete" method="POST">
                    <input type="checkbox" name="checkbox" value="<%= item._id %>" onChange="submit()">
                    <p>
                        <%=item.name%>
                    </p>
                </form>
            </div>
            <%});%>


                <form class="item" action="/" method="POST">
                    <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
                    <button type="submit" name="button">+</button>
                </form>

    </div>
</body>

</html>